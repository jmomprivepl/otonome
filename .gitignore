# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env

# Python (training venv — do not commit)
training/.venv/

# qvac-fabric local venv (huge; exceeds GitHub file limits)
qvac-fabric-llm.cpp/venv/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Superpowers brainstorm sessions (local mockups / state)
.superpowers/

# Large BitNet weights (GitHub LFS single-file limit 2GB; keep local only)
ms-bitnet-bf16/model.safetensors
ms-bitnet-raw/model.safetensors
